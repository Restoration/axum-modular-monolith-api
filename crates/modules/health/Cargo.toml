[package]
name = "module-health"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.8"
shared = { path = "../../shared" }

[dev-dependencies]
http = "1"
http-body-util = "0.1"
tokio = { version = "1", features = ["macros"] }
tower = { version = "0.5", features = ["util"] }
